title: The EF Research Log
email: your-email@example.com
description: >-
  Field notes on the alignment problem, stoic philosophy, and the mathematics of wealth. A digital commonplace book.
baseurl: ""
url: "https://ethereal-firmament.github.io"

# Build settings
theme: minima

# Typography & aesthetics (Minima overrides if supported, or just for your ref)
# We'll handle the visual "jazz" in the CSS/layout, but this sets the tone.
header_pages:
  - about.md

# Exclude from processing
exclude:
  - .github/
  - Gemfile
  - Gemfile.lock
